apiVersion: v2
name: tools
description: >-
  The controller-agent Tool + Skill catalog: the Tool and Skill custom resources
  (ADR 0010/0011) the core-controller reconciles and the orchestrator
  RAG-selects. Applied as post-install/post-upgrade hooks so they land after
  the core-controller Deployment (their CRDs ship in that chart's crds/ dir).
type: application
version: 0.1.0
appVersion: "0.1.0"
home: https://github.com/imaustink/agent-controller
sources:
  - https://github.com/imaustink/agent-controller
keywords:
  - agent
  - catalog
  - crd
maintainers:
  - name: controller-agent
