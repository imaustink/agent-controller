apiVersion: v1
kind: Service
metadata:
  name: {{ include "connection-broker.fullname" . }}
  labels: {{- include "connection-broker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "connection-broker.selectorLabels" . | nindent 4 }}
