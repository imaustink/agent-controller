---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: integrationroutes.core.controller-agent.dev
spec:
  group: core.controller-agent.dev
  names:
    kind: IntegrationRoute
    listKind: IntegrationRouteList
    plural: integrationroutes
    singular: integrationroute
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IntegrationRoute is the Schema for the integrationroutes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of IntegrationRoute
            properties:
              agentRef:
                description: |-
                  agentRef names an Agent CR (same namespace) to dispatch to directly
                  (docs/adr/0021), same as a Skill's agentRefs. Exactly one of
                  skillRef/agentRef/toolRef must be set.
                type: string
              match:
                description: match selects which inbound events this route applies
                  to.
                properties:
                  action:
                    description: |-
                      action is the adapter-specific sub-action (e.g. "assigned"). Omitted
                      matches any action for this source/event pair.
                    type: string
                  event:
                    description: event is the adapter-specific event name (e.g. "issues").
                    minLength: 1
                    type: string
                  source:
                    description: source is the adapter that produced the event (e.g.
                      "github").
                    minLength: 1
                    type: string
                required:
                - event
                - source
                type: object
              promptTemplate:
                description: |-
                  promptTemplate is the request sent to the target, rendered by
                  substituting `{{field}}` placeholders with the matched event's fields
                  (e.g. owner, repo, issueNumber, title, body, senderLogin,
                  labelName — the exact set depends on the adapter that produced
                  the event).
                minLength: 1
                type: string
              skillRef:
                description: |-
                  skillRef names a Skill CR (same namespace) to dispatch to. Exactly one
                  of skillRef/agentRef/toolRef must be set.
                type: string
              toolRef:
                description: |-
                  toolRef names a Tool CR (same namespace) to dispatch to. Exactly one
                  of skillRef/agentRef/toolRef must be set.
                type: string
            required:
            - match
            - promptTemplate
            type: object
            x-kubernetes-validations:
            - message: exactly one of skillRef, agentRef, or toolRef must be set
              rule: (has(self.skillRef)?1:0)+(has(self.agentRef)?1:0)+(has(self.toolRef)?1:0)==1
          status:
            description: status defines the observed state of IntegrationRoute
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the IntegrationRoute resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
