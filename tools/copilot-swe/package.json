{
  "name": "copilot-swe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Privileged subagent container: a software-engineering task in, a GitHub pull request out — wraps the agentic GitHub Copilot CLI.",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "copilot-swe": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@recipe-agent/messaging": "0.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^4.0.0"
  }
}
