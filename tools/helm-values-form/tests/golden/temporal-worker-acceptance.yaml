# Rendered by scripts/helm-check.mjs from the temporal-worker/acceptance case.
# Regenerate with: npm run helm-check:update
#
# Values file the form produced:
#   # Generated by helm-values-form for chart: temporal-worker
#   # Schema version: 0.1.0
#   # Only values that differ from the chart defaults are listed.
#   image:
#     tag: 1.42.3
#   resources:
#     limits:
#       memory: 1Gi
---
# Source: temporal-worker/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rel-temporal-worker
  labels:
    app.kubernetes.io/name: temporal-worker
    app.kubernetes.io/instance: rel
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
---
# Source: temporal-worker/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rel-temporal-worker
  labels:
    app.kubernetes.io/name: temporal-worker
    app.kubernetes.io/instance: rel
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: temporal-worker
      app.kubernetes.io/instance: rel
  template:
    metadata:
      labels:
        app.kubernetes.io/name: temporal-worker
        app.kubernetes.io/instance: rel
    spec:
      serviceAccountName: rel-temporal-worker
      containers:
        - name: worker
          image: "ghcr.io/example/temporal-worker:1.42.3"
          imagePullPolicy: IfNotPresent
          env:
            - name: TEMPORAL_ADDRESS
              value: "temporal-frontend.temporal.svc.cluster.local:7233"
            - name: TEMPORAL_NAMESPACE
              value: "default"
            - name: TEMPORAL_TASK_QUEUE
              value: "worker-tq"
            - name: TEMPORAL_MAX_CONCURRENT_ACTIVITIES
              value: "100"
          resources:
            limits:
              cpu: 500m
              memory: 1Gi
            requests:
              cpu: 100m
              memory: 128Mi
