{
  "name": "@controller-agent/agent-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SDK for building sub-agents: NATS-backed bidirectional loop scaffolding (init, prompts, progress, HITL ask, graceful exit) over the @controller-agent/messaging agent protocol.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@controller-agent/messaging": "0.1.0",
    "nats": "2.29.3"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "typescript": "^5.6.2",
    "vitest": "^4.0.0"
  }
}
