{
  "name": "@controller-agent/github-app-auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitHub App JWT signing and installation-token minting, shared by any workspace member that needs to act as the controller-agent GitHub App (ADR 0018).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "typescript": "^5.6.2",
    "vitest": "^4.0.0"
  }
}
