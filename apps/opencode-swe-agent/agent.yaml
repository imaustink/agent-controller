# Agent custom resource for the opencode-swe-agent -- apply with:
#   kubectl apply -n <namespace> -f apps/opencode-swe-agent/agent.yaml
#
# Replaces both the old `copilot-swe` Tool CR (tools/copilot-swe/tool.yaml)
# and the `software-engineering-skill` Skill CR
# (apps/agent-orchestrator/config/samples/software-engineering-skill.yaml).
# When applying this, delete the old Tool and Skill CRs:
#   kubectl -n <namespace> delete tool copilot-swe
#   kubectl -n <namespace> delete skill software-engineering-skill
#
# Prerequisites:
#   kubectl -n <namespace> create serviceaccount opencode-swe-agent
#
#   Option A -- fine-grained PAT (default, shown in secretEnv below):
#   kubectl -n <namespace> create secret generic opencode-swe-secrets \
#     --from-literal=GITHUB_TOKEN=github_pat_... \
#     --from-literal=ANTHROPIC_API_KEY=sk-ant-...
#
#   Option B -- GitHub App installation token (docs/adr/0018): mints a
#   short-lived (~1h) token per run instead of using a long-lived PAT. Set
#   ALL THREE of GITHUB_APP_ID/GITHUB_APP_PRIVATE_KEY/GITHUB_APP_INSTALLATION_ID
#   (a partial set is rejected) and drop GITHUB_TOKEN, or leave GITHUB_TOKEN as
#   a fallback -- the App credentials take precedence when all three are set.
#   kubectl -n <namespace> create secret generic opencode-swe-secrets \
#     --from-literal=GITHUB_APP_ID=123456 \
#     --from-literal=GITHUB_APP_PRIVATE_KEY="$(cat github-app-private-key.pem)" \
#     --from-literal=GITHUB_APP_INSTALLATION_ID=987654 \
#     --from-literal=ANTHROPIC_API_KEY=sk-ant-...
#   ...then add secretEnv entries for GITHUB_APP_ID, GITHUB_APP_PRIVATE_KEY,
#   and GITHUB_APP_INSTALLATION_ID (same secretRef.name, matching keys) below.
apiVersion: core.controller-agent.dev/v1alpha1
kind: Agent
metadata:
  name: opencode-swe-agent
spec:
  description: >-
    Performs software-engineering work on GitHub end-to-end. Runs the
    opencode coding-agent CLI — which has bash, file-read, grep, and glob
    tools — plus the git and gh (GitHub CLI) command-line tools, so it can
    clone, browse, and read any repository's files and history, not only
    write to them. That means it covers both: (1) read-only questions
    about what is actually in a repository, e.g. "how many levels does my
    lander game have, check GitHub" or "what does this repo's config
    say" — it clones/inspects the repo and answers directly, no code
    change required; and (2) changes — creating or cloning repositories,
    implementing features and fixes, writing code and tests, and opening
    or updating pull requests. Handles multi-turn refinement — follow-up
    instructions push more commits to the same pull request. Supports
    human-in-the-loop clarifying questions when the task needs more
    context before it can proceed.
  input: >-
    A natural-language software-engineering instruction or question about
    a GitHub repository's contents (e.g. "create a repo called hello-svc
    with a Node HTTP server and open a PR", or "how many levels does my
    lander-game repo have?"), or a follow-up refinement instruction (e.g.
    "add a health check endpoint") for a session that is already in
    progress.
  output: >-
    A summary of the changes plus a link to the opened/updated pull
    request when the task changed code; a direct answer when the task
    was only a question about a repository's contents; or a mid-turn
    clarifying question to the user if more context is needed before
    work can begin.
  # Coding work WRITES to GitHub, so writer role is required.
  allowedRoles:
    - writer
  tier: privileged
  image: opencode-swe-agent:latest
  serviceAccountName: opencode-swe-agent
  # -- Orchestrator prompt (injected into the PARENT orchestrator's planner) --
  # Explains how and when to delegate to this agent, and how to interpret
  # the swe marker it embeds in its replies.
  orchestratorPrompt: |
    Delegate to this agent for any software-engineering request AND for any
    question that requires actually looking at a GitHub repository's
    contents to answer — it has git/gh CLIs plus file read/search tools, so
    it can clone and inspect a repo instead of guessing. This covers both:
    creating or cloning repositories, implementing features, fixing bugs,
    writing tests, and opening or updating pull requests; and answering
    read-only questions about what is in a repo (e.g. "how many levels
    does my lander game have, check GitHub" or "what does this repo's
    README say").

    Pass the user's request verbatim as the goal.

    If the previous assistant message starts with an HTML comment like
    <!-- swe: repo=... branch=... pr=... session=... -->, the agent is
    continuing an existing session. Include that FULL text at the start
    of the goal so the agent can resume the same pull request.

    The agent may pause mid-task to ask you a clarifying question (a
    non-final reply in the chat). Answer naturally; the conversation
    will resume automatically.
  # -- Agent prompt (the sub-agent's own internal system prompt) --
  # Loaded by the agent-runtime SDK at startup from AGENT_PROMPT env
  # (injected by the controller from this field in Phase 7).
  agentPrompt: |
    You are a software-engineering sub-agent that reads, writes, and manages
    GitHub repositories using the opencode CLI (calling Claude Sonnet 5 via
    the Anthropic API, with its bash/file-read/grep/glob tools) and the
    gh/git CLIs. You are spawned for a single task and communicate with the
    orchestrator over NATS.

    Your job:
    1. Authenticate with the provided GITHUB_TOKEN and ANTHROPIC_API_KEY.
    2. Run the opencode CLI to satisfy the request. Not every task needs a
       code change: if it's a read-only question about a repository's
       contents (e.g. "how many levels does this repo's game have"), clone
       or otherwise inspect the repo with git/gh and answer directly — you
       do not need to commit or open a PR unless the task actually calls
       for a change.
    3. Report progress to the orchestrator as you go.
    4. When you have a result, return the PR link and a summary for a
       code change, or the direct answer for a read-only question.
    5. If the task is genuinely ambiguous and you cannot proceed without
       clarification, ask the user exactly ONE question using the ask()
       mechanism, wait for their answer, then proceed.

    Never ask for clarification if you can make a reasonable default
    choice. Never perform irreversible operations (force-push, delete
    branches/repos).
  # Coding tasks take much longer than the 300s default.
  # agentRunTimeoutSeconds (1800) set at the orchestrator level covers this,
  # but the Go controller also reads timeoutSeconds here to set the Job's
  # activeDeadlineSeconds (when that field is added to AgentSpec -- for now
  # the orchestrator-level agentRunTimeoutSeconds applies).
  resources:
    requests:
      cpu: "500m"
      memory: "512Mi"
      ephemeral-storage: "2Gi"
    limits:
      cpu: "2"
      memory: "2Gi"
      ephemeral-storage: "8Gi"
  secretEnv:
    - name: GITHUB_TOKEN
      secretRef:
        name: opencode-swe-secrets
        key: GITHUB_TOKEN
    - name: ANTHROPIC_API_KEY
      secretRef:
        name: opencode-swe-secrets
        key: ANTHROPIC_API_KEY
